import svars from '../styles/colors.scss';

const colors = {
    green: svars.colorGreen,
    orange: svars.colorOrange,
    blue: svars.colorBlue,
    gray: svars.colorGray
}

export default colors;
